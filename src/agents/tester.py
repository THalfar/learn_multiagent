def tester_node(state):
    """
    Tester runs tests on the code, especially training CartPole.
    """
    # In real: exec code, run episodes, measure reward
    return {
        "test_results": "Placeholder: Average reward 20.0 over 10 episodes (below threshold)."
    }